# Retirement Hacienda — shared site
Static site (no build step). `index.html` is the friendly overview; report/scorecard/map are linked from it.
Publish free with GitHub Pages: create a repo, upload these files, then Settings -> Pages -> Deploy from branch -> main / root.
